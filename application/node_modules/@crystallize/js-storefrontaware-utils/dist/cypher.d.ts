export declare function encryptValue(value: string, secretKey: string, algorithm: string): string;
export declare function decryptValue(value: string, secretKey: string, algorithm: string): string;
/**
 * Create encrypt/decrypt/decryptMape methods based on supplied escret
 */
export declare const cypher: (secret: string) => {
    encrypt: (value: string) => string;
    decrypt: (value: string) => string;
    decryptMap: (map: {
        [key: string]: string;
    }) => {
        [key: string]: string;
    };
};
